{
    "name": "cyber-mystery-lunch",
    "version": "0.1.0",
    "private": true,
    "scripts": {
      "dev": "wrangler dev worker/index.ts --local --site public",
      "deploy": "wrangler deploy worker/index.ts --minify --site public",
      "format": "prettier -w .",
      "typecheck": "tsc --noEmit"
    },
    "devDependencies": {
      "typescript": "^5.6.2",
      "wrangler": "^3.79.0",
      "prettier": "^3.3.3"
    }
  }
  